reate the AWS Glue job

Go to:

AWS Console → AWS Glue → ETL jobs / AWS Glue Studio

Create a new job.

For this starter project, choose a Python Shell job rather than Spark.

Python Shell is appropriate because what you're doing is primarily
HTTP API call
JSON processing
S3 upload
